<!DOCTYPE html>
<html lang="nl">
<head>
<meta charset="UTF-8">
<title>FIP Live Metadata</title>
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<style>
  body {
    font-family: Arial, sans-serif;
    background: linear-gradient(135deg, #1d1d1d, #3a3a3a);
    color: white;
    text-align: center;
    padding: 20px;
  }
  h1 {
    font-size: 2rem;
    margin-bottom: 10px;
  }
  h2 {
    font-size: 1.5rem;
    margin: 5px 0;
    animation: fadeIn 0.5s ease;
  }
  h3 {
    font-size: 1.2rem;
    font-weight: normal;
    color: #ff80aa;
    margin: 0;
    animation: fadeIn 0.5s ease;
  }
  img {
    max-width: 300px;
    width: 100%;
    border-radius: 15px;
    margin-top: 15px;
    box-shadow: 0 4px 15px rgba(0,0,0,0.5);
    transition: transform 0.3s ease;
  }
  img:hover {
    transform: scale(1.05);
  }
  @keyframes fadeIn {
    from { opacity: 0; transform: translateY(10px); }
    to { opacity: 1; transform: translateY(0); }
  }
  footer {
    font-size: 0.8rem;
    margin-top: 20px;
    opacity: 0.6;
  }
</style>
</head>
<body>
  <h1>🎵 FIP Live</h1>
  <h2 id="title">Laden...</h2>
  <h3 id="artist"></h3>
  <img id="cover" src="" alt="Albumcover">
  
  <footer>Data via fip-metadata.fly.dev – Automatisch ververst</footer>

  <script>
    async function updateFIP() {
      try {
        const res = await fetch("https://fip-metadata.fly.dev/api/metadata/fip");
        const data = await res.json();

        const current = data.now?.title || "Onbekend nummer";
        const artist = data.now?.artist || "Onbekende artiest";
        const cover = data.now?.cover || "";

        document.getElementById("title").textContent = current;
        document.getElementById("artist").textContent = artist;
        document.getElementById("cover").src = cover;
      } catch (err) {
        console.error("Fout bij laden FIP metadata", err);
      }
    }
    updateFIP();
    setInterval(updateFIP, 10000); // elke 10 seconden verversen
  </script>
</body>
</html>
